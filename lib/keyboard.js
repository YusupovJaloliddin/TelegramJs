// const { Markup } = require("telegraf");

// const keyboards={
//     "start": Markup.inlineKeyboard([Markup.callbackButton("Komandalarni ko'rsatish","help")])
// }
// module.exports=keyboards

const { Markup } = require("telegraf");

const keyboards={
    "start": Markup.inlineKeyboard([Markup.callbackButton("Komandalarni ko'rsatish","help")])
}
module.exports=keyboards